Keep updating:
When you make code changes locally, just run:

git add .
git commit -m "update dashboard"
git push

That updates your GitHub automatically.

>>>>

SMS work pending

>>>>

pip freeze > requirements.txt

git add requirements.txt
git commit -m "Add requirements.txt"
git push

